import { PagePlaceholder } from "../page";

export default function PublishPage() {
  return (
    <PagePlaceholder
      title="Publish Book"
      subtitle="A simple placeholder for authors to publish new books."
    />
  );
}
